import "./Cart.css"
 const Cart = (product) => {
  console.log(product.id);
  return (
    <div className="card">
     <h2>Your cart need to updated.</h2>
     
    </div>
  );
};

export default Cart;


